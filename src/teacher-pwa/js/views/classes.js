Views.classes=async function(){const app=document.getElementById('app');const courses=await IDB.getAll('courses');const myCourses=courses.filter(c=>c.teacher_ids.includes(State.currentTeacherId));app.innerHTML=`${UI.createHeader('Mis Cursos')}<div class="container"><div class="grid gap-2">${myCourses.map(c=>`<div class="card"><div class="card-header">${c.name} - ${c.grade}</div><div class="flex gap-1"><button class="btn btn-primary" onclick="Views.classes.selectCourse(${c.id})">Abrir Nómina</button><button class="btn btn-secondary" onclick="Views.classes.takeAttendance(${c.id})">Tomar Asistencia</button></div></div>`).join('')}</div><div class="mt-2"><button class="btn btn-secondary" onclick="Router.navigate('/settings')">Configuración</button><button class="btn btn-secondary" onclick="State.logout();Router.navigate('/auth')">Cerrar Sesión</button></div></div>${UI.createBottomNav('/classes')}`;Views.classes.selectCourse=function(id){State.setSession(State.currentTeacherId,id);Router.navigate('/roster')};Views.classes.takeAttendance=function(id){State.setSession(State.currentTeacherId,id);Router.navigate('/take-attendance')}};
