Views.settings=async function(){const app=document.getElementById('app');const online=State.isOnline();app.innerHTML=`${UI.createHeader('Configuración')}<div class="container"><div class="card"><div class="card-header">Conectividad</div><div class="flex items-center justify-between"><span>Estado: ${online?'Online':'Offline'}</span><button class="btn btn-secondary" onclick="Views.settings.toggleOnline()">${online?'Simular Offline':'Simular Online'}</button></div></div><div class="card"><div class="card-header">Device ID</div><div>${State.deviceId}</div></div><div class="card"><div class="card-header">Datos</div><button class="btn btn-secondary" onclick="Views.settings.reset()">Resetear Datos</button></div><button class="btn btn-secondary" onclick="Router.navigate('/classes')">← Volver</button></div>`;Views.settings.toggleOnline=function(){State.toggleOnline();UI.showToast(`Ahora ${State.isOnline()?'Online':'Offline'}`,'info');Views.settings()};Views.settings.reset=async function(){if(confirm('¿Resetear todos los datos?')){await IDB.clear('queue');await IDB.clear('attendance_local');State.logout();UI.showToast('Datos reseteados','success');Router.navigate('/auth')}}};
