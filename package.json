{
  "name": "school-attendance",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate": "make migrate",
    "test:kiosk": "node src/kiosk-app/tests/service-worker-consistency.test.js",
    "test:kiosk:e2e": "npx playwright test",
    "test:kiosk:all": "npm run test:kiosk && npm run test:kiosk:e2e",
    "serve:kiosk": "npx serve src/kiosk-app -l 8080 -s"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0",
    "serve": "^14.2.5"
  }
}
